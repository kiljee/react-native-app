import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 20,
    alignItems: 'center',
  },
  image: {
    width: 150,
    height: 200,
    borderRadius: 16,
    marginBottom: 12,
  },
  text: {
    marginBottom: 12,
    fontSize: 16,
    fontWeight: '600',
    color: '#686969',
  },
});

export default style;
